# Local development environment variables

# Base URL for the Configuration API
CONFIG_API_BASE_URL=http://config-api-svc/api/v1

# Quix SDK token - used for both UI login and API authentication
# Get this from Quix portal (Settings → Personal Access Token)
Quix__Sdk__Token=your-sdk-token
